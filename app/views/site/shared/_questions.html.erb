<% @questions.each do |question| %>
<div class="card mb-3">
  <h5 class="card-header"><%= question.description %></h5>
  <div class="card-body">
    <ul>
      <% question.answers.each do |answer| %>
        <li><%= answer.description %></li>
      <% end %>
    </ul>
  </div>
</div>
<% end %>

